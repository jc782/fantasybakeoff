{% extends 'base.html' %}

{% block head_title %}{{league.league}} | {{block.super}} {% endblock head_title %}


{% block content %}
<h3>{{league.league}}</h3><br>
<h4>Share this league number {{league.id}}</h4><br>
<table class="table">
	<thead>
		<tr>
			<th>Team</th>
			<th>Contestant</th>
			<th>Score</th>
		</tr>
	</thead>
	<tbody>
{% league.teams.all.order_by(get_score) %}
{% for team in league.teams.all %} 
		<tr>
			<td>{{team}}</td>
			<td>{{team.user.first_name}} {{team.user.last_name}}</td>
			<td>{{ team.get_score }}</td>
		</tr>
{% endfor %}
	</tbody>
</table>
{{league.timestamp}}

{% endblock content %}
